import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firefoxGT108',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
